# Kali 虚拟机扩容与恢复复盘

> 根据本次对话、终端输出和截图整理。最终用户确认修复成功。本文是这台虚拟机的操作记录，不是可以直接照抄到其他磁盘的分区脚本。

## 一、排查思路：先判断故障发生在哪一层

这次不是一个单独的“分区错误”，而是一串相互关联的问题：

```text
软件包升级时根分区空间不足
  → MariaDB 升级失败，480 个软件包未完成安装或配置
  → 重启后出现黑屏，lightdm 服务失败
  → 发现虚拟磁盘已有 80 GiB，但根分区只有 18.9 GiB
  → 用 GParted Live 调整分区
  → 缩小扩展分区时反复触发 GParted 边界检查错误
  → 对外层扩展分区改用 None 对齐后成功加入操作队列
  → 扩大根分区并回到 Kali
  → 修复软件包时遇到网络不通
  → 激活 eth0，恢复默认路由和 DNS
  → 完成软件包修复
```

这里有两个重要的证据边界：

- 根分区满、软件包未完成、lightdm 失败都已由输出证实；空间不足及升级中断是黑屏的合理解释，但没有取得 lightdm 日志，不能断言它是唯一原因。
- GParted 在正确输入数值后仍拒绝操作，而改用 `None` 对齐后操作被接受，说明故障与该操作的对齐或边界计算有关。没有内部调试日志，不能把它定性为某个已确认版本缺陷。

最初“虚拟机找不到”的调查与后续修复应分开：Windows 中原 `Kali_Purple` 目录为空，但没有完成全盘搜索，VMware 配置目录也不可读，因此不能证明原虚拟机已删除，也不能证明后面运行的 Kali 就是最初那一台。

### 本次应优先掌握的概念

按你在对话中反复确认的内容安排篇幅，不对你的能力打分：

1. **虚拟磁盘、分区和文件系统不是同一个容量；扩容为什么受相邻空间限制。**
2. **ISO 是启动/安装介质，已经装好的 Kali 在虚拟硬盘上；挂载不等于从它启动。**
3. **GParted 预览、待执行操作和已经写入磁盘的状态必须区分。**
4. **有网卡不等于能上网；IP、默认路由、DNS 要分层检查。**
5. **软件包故障要先修复根因，再完成被中断的安装。**

## 二、完整操作过程：按实际时间顺序

### 1. 查找虚拟机与进入安装流程

Windows 中找到了 `C:\Users\agg\Documents\Virtual Machines\Kali_Purple`，但目录为空。常见位置还发现 Kali 安装 ISO；安装镜像并不是旧虚拟磁盘，不包含原虚拟机中的项目和设置。最初查找未得出“旧虚拟机已删除”的结论。

之后你进入 Kali 安装流程。对话给出了普通虚拟机的配置建议：NAT 网络、普通用户、默认工具集和 Xfce；只有确认是空白虚拟磁盘时才使用整盘自动分区。不能仅凭这些建议断言所有选项都按此执行。

### 2. MariaDB 升级因空间不足中断

执行：

```bash
sudo apt --fix-broken install
```

关键输出：

```text
480 not fully installed or removed.
ERROR: There's not enough space in /var/lib/mariadb/
new mariadb-server package preinst maintainer script subprocess failed
```

这说明失败发生在 MariaDB 解包前的脚本中，不是先证明数据库内容损坏。该版本脚本检查数据目录所在文件系统可用空间；不足约 4 MiB 就退出。这个阈值只是最低检查门槛，不是完成升级所需空间的估算。

### 3. 黑屏后从文字终端检查

虚拟机开机后停在黑屏光标。进入文字终端登录，运行：

```bash
df -h
lsblk -o NAME,SIZE,FSTYPE,MOUNTPOINTS
sudo systemctl --failed --no-pager
```

发现：

| 检查项 | 当时的证据 | 含义 |
|---|---|---|
| `/dev/sda` | 80 GiB | 虚拟硬盘已经变大 |
| `/dev/sda1` | 18.9 GiB，挂载到 `/` | 系统分区没有随之变大 |
| `/` 可用容量 | 0，使用率 100% | 系统文件所在分区满了 |
| `/dev/sda5` | 约 1.1 GiB，swap | 交换分区位于系统分区后面 |
| `lightdm.service` | failed | 图形登录服务失败，但文字终端仍可工作 |

### 4. 清缓存，读取分区布局

执行 `sudo apt clean` 后，根分区可用空间增加到约 2.7 GB。该命令删除下载的软件包缓存，不卸载已安装软件；后续修复需要重新下载包。

中间曾把 `df -h /` 和 `fdisk -l /dev/sda` 输到同一行，`df` 把 `fdisk` 当成路径，因此出现 `df: fdisk: No such file or directory`。正确做法是分行执行：

```bash
df -h /
sudo fdisk -l /dev/sda
```

`-l` 是小写字母 L，表示列出分区。

原布局为 MBR/msdos 分区表：

```text
[ sda1：系统，约 19 GiB ][ sda2：包含 sda5 交换分区，约 1 GiB ][ 未分配：60 GiB ]
```

80 GiB 是磁盘容量，系统只能使用已分配给根分区且文件系统已扩展的部分。

### 5. 用 GParted Live 从虚拟光驱启动

选择 GParted ISO 后第一次仍进入 Kali，文件管理器能看到光盘文件。这只说明镜像被挂载，并不说明从它启动了。

实际采用的路径：

1. 关闭虚拟机；在分区写入前准备虚拟机备份。
2. VMware 的 CD/DVD 选择 GParted Live amd64 ISO，启用 `Connect at power on`。
3. 使用 `Power On to Firmware` 进入固件。
4. 在 PhoenixBIOS 的 Boot 页面，把 `CD-ROM Drive` 移到硬盘之前，按 F10 保存。
5. 在 GParted Live 启动过程中选择 `Don't touch keymap`，保留默认语言和模式 `0`，进入图形分区工具。

Live 环境从 ISO 运行，因此可以在不使用已安装 Kali 根分区的情况下调整它。没有必要重新安装 Kali。

### 6. 先移动交换分区，再扩大系统分区

不能直接扩大 `sda1`，因为空闲空间不与它相邻。`sda2` 是装着逻辑分区 `sda5` 的容器，不是额外的一块物理磁盘。

计划顺序：

```text
① 扩大 sda2 外层容器，使其包含后面的空闲空间
② 把 sda5 移到容器末尾，保持交换空间约 1.08 GiB
③ 从左侧缩小 sda2，释放中间的空闲空间
④ 把 sda1 的右边界扩大到空闲空间末端
```

第一步把 `sda2` 扩到 61.08 GiB，**并不是分配 61 GB 的交换空间**。交换空间大小由内部 `sda5` 决定。

操作中的偏差和修正：

- 某次预览中 `sda5` 占满外层容器，导致 `sda2` 无法缩小；该次待执行操作被撤销。
- 某次 `sda5` 的 New size 被改成 1000 MiB；随后恢复到约 1102 MiB，并移动到末尾。
- 移动分区应拖色块中间；调整大小应拖边缘手柄。输入前方空闲后，要检查 New size 是否也正确变化，不能只检查一个字段。
- 后来出现一条额外的“向左移动 sda5”待执行操作，它不是目标步骤，应撤销。

### 7. GParted 边界错误与最终绕过方式

缩小 `sda2` 时多次出现：

```text
Could not add this operation to the list
GParted Bug: A partition cannot end (165511168) after the end of the device (%2)
```

含义是当前操作未能通过校验、没有加入队列；不能仅凭此提示认定磁盘已损坏。`%2` 未被替换为实际数值，也不应拿该提示当作准确的磁盘容量报告。

尝试过的办法包括调整数值、改用拖动、分步应用已有操作和刷新设备。它们没有消除该错误。此前反复将问题归因于用户拖动方式并不充分；正确参数下仍可复现，应及时转向读取真实分区表。

随后在 Live 终端只读检查：

```bash
sudo fdisk -l /dev/sda
sudo parted /dev/sda unit s print free
```

当时已写入磁盘的关键数值：

| 对象 | 起始扇区 | 结束扇区 |
|---|---:|---:|
| 整盘 | 0 | 167772159 |
| sda1 | 2048 | 39684095 |
| sda2 | 39686142 | 167772159 |
| sda5 | 165515262 | 167772159 |

扇区为 512 字节。输出证明交换分区已经真正位于磁盘末尾，而不是仅在预览中移动。外层容器也延伸到了磁盘最后一个扇区。

最后采用：

1. 对 **sda2 外层容器**选择 `Align to: None`。
2. 右边界不动，收缩左边界；容器保留约 1104 MiB。
3. 操作成功加入队列，预览显示容器内交换分区前还保留约 2 MiB。
4. 对 **sda1** 保持 `MiB` 对齐，仅扩大右边界，目标约 78.92 GiB。
5. 核对队列后应用操作；用户确认完成。

**结论：这是扩大外层容器后的收缩步骤触发的 GParted 对齐/边界校验问题，并不是“临时扩大容器必然破坏分区”。** `None` 是本次核对结构之后使用的局部绕过方式，不应推广为所有分区的默认设置。官方说明它不保证为引导记录预留空间；本次保留的容器内间隙因此很重要。

### 8. 回到 Kali：ISO 不再需要连接

完成后关闭 Live 环境，断开虚拟光驱，重新从硬盘启动 Kali。

你多次确认 ISO 是否需要改回 Kali。答案是：**不需要。** ISO 文件路径可以继续显示 GParted，只要光驱不连接，就不参与启动。

| 选项 | 含义 |
|---|---|
| Connected | 当前虚拟光驱是否连接 |
| Connect at power on | 下次开机是否自动连接 |
| ISO 路径 | 虚拟光驱连接时使用哪张镜像 |
| BIOS 启动顺序 | 先尝试从哪个设备启动 |

换成 Kali ISO 本身不会格式化硬盘，但如果从安装盘启动并确认安装、格式化分区，就可能覆盖现有系统。日常使用不需要安装 ISO。

备份与当前虚拟机文件要区分：待容量、重启、软件包和项目数据均验证正常后，再考虑删除复制出来的备份文件夹。当前使用的 `.vmdk` 和 `.vmx` 不能当作安装文件删除；VMware 快照应通过快照管理器处理。

### 9. 软件包修复遇到网络故障

再次执行修复命令时出现：

```text
Temporary failure resolving 'http.kali.org'
```

最初表现为 DNS 解析失败，但进一步检查发现没有默认路由：

```bash
ip route
ping -c 3 1.1.1.1
getent hosts http.kali.org
nmcli device status
ip -br address
```

证据为：`eth1` 有 `192.168.186.129/24`，`eth0` 显示 disconnected 且无 IP，ping 返回 `Network is unreachable`。VMware 中 NAT 网卡虽然已勾选连接，但 Kali 内的网络连接尚未激活。

执行：

```bash
sudo nmcli device connect eth0
```

成功后观察到：

- `eth0` 获得 `192.168.236.128`。
- 默认路由为 `default via 192.168.236.2 dev eth0`。
- ping `1.1.1.1` 三次全部成功。
- `getent hosts http.kali.org` 返回地址。

这证明当时外网连接和域名解析恢复。不要把这些地址复制到另一台虚拟机；它们由本次 VMware NAT 网络分配。最终输出不再显示此前的 eth1 路由，但对话没有确认其原因，不能据此判断 Host-only 网络也已验证正常。

### 10. 完成软件包修复

网络恢复后，给出的执行顺序为：

```bash
sudo apt --fix-broken install
sudo dpkg --configure -a
sudo apt-get check
sudo reboot
```

每条成功后才继续下一条。用户随后报告“成功了”；对话没有展示最终各条命令的全部输出或最终 `df` 截图，因此准确结论是：用户确认整体修复成功，最终容量与软件包状态没有逐项输出存档。

## 三、术语、参数与操作原因

### 1. 最重要：容量有三层

**虚拟磁盘 → 分区 → 文件系统。** VMware 扩大磁盘只提供新的扇区；分区边界和 ext4 文件系统还要扩展，Kali 才能使用新增容量。GParted 对受支持的文件系统扩容会协调这两个步骤。

`/dev/sda` 是整盘；`sda1` 是系统分区；`/` 是根文件系统挂载点。`sda2` 的 extended 表示 MBR 扩展分区容器；`sda5` 是其中的逻辑交换分区。Swap 是内存压力下使用的磁盘空间，不是项目目录，也不等于真正增加 RAM。

GiB = 1024³ 字节，MiB = 1024² 字节。界面显示约 79 GiB，而 `df` 的可用容量还会受到文件系统元数据、保留空间和已存文件影响，数值不必完全一样。

### 2. 预览不是执行

`Resize/Move` 把操作加入队列；绿色 `Apply` 才执行写盘。待执行操作可以撤销；已应用操作不能靠撤销按钮回滚。应用失败时应先查看 Details，不能假定所有步骤都未执行。

相邻空闲空间必须处于正确的分区层级：空闲空间若仍在 `sda2` 内，就不能直接给外部 `sda1` 使用。列表缩进和外层边框能帮助判断。

### 3. 常用命令的作用

| 命令或参数 | 用途 |
|---|---|
| `df -h /` | 看根文件系统容量；`-h` 使用易读单位 |
| `lsblk -o ...` | 看磁盘、分区和挂载关系；`-o` 指定列 |
| `fdisk -l /dev/sda` | 只读列出指定磁盘分区表 |
| `parted ... unit s print free` | 用扇区单位列出分区和空闲区，不修改分区 |
| `systemctl --failed --no-pager` | 列出失败服务，不进入分页界面 |
| `apt clean` | 清除下载缓存，不卸载软件 |
| `apt --fix-broken install` | 让 APT 尝试解决依赖并完成所需安装；先查看拟议变更 |
| `dpkg --configure -a` | 配置所有已解包但尚未配置的软件包 |
| `apt-get check` | 检查软件包依赖状态 |
| `ip route` | 看路由，尤其是否存在默认出口 |
| `ip -br address` | 简要列出接口状态和 IP 地址 |
| `ping -c 3` | 发三次连通性探测；失败不总能独立证明断网 |
| `getent hosts` | 经系统名称解析机制查询主机地址 |
| `nmcli device connect eth0` | 让 NetworkManager 激活指定接口的连接 |

终端提示符为 root 的 `#` 时通常不需要再加 sudo；普通用户的 `$` 下，管理操作使用 sudo。不要复制提示符本身。

### 4. 网络四个层次

VMware 的“Connected”表示虚拟网线连接；Kali 内还需要接口连接配置、IP 和路由。NAT 用于经宿主机访问外网；Host-only 通常只用于宿主机与虚拟机之间的私有网络。默认路由决定访问其他网段走哪一个网关；DNS 将域名转换成地址。

本次先修复 eth0 连接，DNS 随之恢复，因此无需先换软件源或手动写 DNS。`--fix-missing` 也不能修复缺失的默认路由。

## 四、问题清单：先按时间，再按类别

### 按发生时间

| 编号 | 问题 | 最终处理/状态 |
|---|---|---|
| 01 | 最初找不到 Kali 虚拟机 | 旧目录为空；调查未能确定旧文件去向 |
| 02 | 不熟悉安装选项和容量 | 给出配置建议；项目用途建议预留更多磁盘空间 |
| 03 | MariaDB 升级报空间不足 | 清缓存，随后扩容根分区 |
| 04 | 黑屏、lightdm failed | 从文字终端排查，修复空间和安装状态 |
| 05 | 80 GiB 磁盘却只有 19 GiB 系统空间 | 区分磁盘、分区、文件系统 |
| 06 | 两条命令误输入同一行 | 分行运行 df 与 fdisk |
| 07 | 挂载 GParted ISO 后仍进入 Kali | 修改光驱启动顺序 |
| 08 | 不清楚键盘与 Live 模式选择 | 保留默认键盘，选择图形模式 0 |
| 09 | 为什么不直接扩大 sda1 | 交换分区隔开空闲空间，先调整布局 |
| 10 | 移动与缩放混淆、swap 大小变化 | 撤销待执行操作，核对大小和位置 |
| 11 | GParted 边界错误反复出现 | 读取真实扇区；仅 sda2 使用 None 对齐后成功 |
| 12 | 不清楚预览与已执行状态 | 用操作队列和 fdisk/parted 区分 |
| 13 | ISO 是否要换回 Kali、备份是否可删 | 日常断开 ISO；验证后再清理备份 |
| 14 | APT 域名解析失败 | 继续检查路由，而非只修改 DNS |
| 15 | NAT 已连接但 eth0 无地址 | nmcli 激活 eth0，恢复出口和解析 |
| 16 | 480 个包未完成安装或配置 | 网络恢复后完成 APT/dpkg 修复；用户确认成功 |

### 按类型归类（保留时间编号）

- **虚拟机文件与备份**：01、13。
- **安装与容量规划**：02、05。
- **空间不足、软件包与桌面故障**：03、04、16。
- **命令输入与工具理解**：06、08、12。
- **启动介质与 BIOS**：07、13。
- **分区结构与 GParted 操作**：09、10、11、12。
- **网络与 DNS**：14、15。

## 五、以后如何减少重复排查

- 在关机状态备份整个虚拟机目录，并确认备份与当前使用的目录不同。
- 做分区修改前保存 `fdisk -l`、`lsblk` 和 `df -h` 输出；不要把本次扇区数字用于别的磁盘。
- 给普通项目预留 100–120 GB、较多容器/抓包预留 150–200 GB 是容量规划建议，并非 Kali 的硬性要求；本次实际磁盘仍为 80 GiB。
- 保留约 20 GB 空闲是实用维护目标，不是 MariaDB 的固定安装需求。
- 出现同样错误且输入已经正确时，应切换到证据核对，不要反复拖动边界。
- 修复完成后再进行下一轮升级；`781 not upgrading` 不等于 781 个软件包损坏。

## 参考资料

- [Kali 安装容量说明](https://www.kali.org/docs/installation/installation-sizes/)
- [MariaDB 11.8.8-1 安装前脚本](https://sources.debian.org/src/mariadb/1%3A11.8.8-1/debian/mariadb-server.preinst)
- [GParted 手册：操作队列、调整大小与对齐](https://gparted.org/display-doc.php?name=help-manual)
- [GParted：在分区之间移动空闲空间](https://gparted.org/display-doc.php?name=moving-space-between-partitions)

实际状态与数值以本次用户提供的截图和终端输出为依据；故障根因的未证实部分已在正文标明。
